{% extends 'base.html.twig' %}

{% block title %}Newsletter{% endblock %}

{% block body %}
    <h1>Newsletter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newsletter.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ newsletter.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ newsletter.email }}</td>
            </tr>
            <tr>
                <th>Date d'Inscription</th>
                <td>{{ newsletter.dateInscription ? newsletter.dateInscription|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('newsletter_index') }}">retour à la liste</a>

    <a href="{{ path('newsletter_edit', {'id': newsletter.id}) }}">modifier</a>

    {{ include('newsletter/_delete_form.html.twig') }}
{% endblock %}
